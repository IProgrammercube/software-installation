pipeline {

    agent any
   
parameters
    {
        choice(
                name: "namespace",
                choices: ["DEV","UAT","PRE"],
                description: "Please select nameSpace"
        )

    }



    stages {

        stage('Print Selection') {
            steps {
                echo "Selected Environment: ${params.ENVIRONMENT}"
            }
        }
    }
}
