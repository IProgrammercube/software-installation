pipeline
{
    agent any
  

    stages
    {
        stage("develop")
        {
               steps{
                     script
                    {

                       
                         sh ''' 
                               declare -A emp

                                emp[name]="Roshan"
                                emp[age]="35"
                                
                                echo "${emp[name]}"
                                echo "${emp[age]}"
                        '''
                    }
               }
        }

    }
}
