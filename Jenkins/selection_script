
pipeline {
    agent any

   

    stages {
        stage('Print Params') {
            steps {
                def envVars = readEnvFile("env")

                echo "App: ${envVars.APP_NAME}"
        q    echo "Env: ${envVars.ENV}"
            }
        }
    }
}
