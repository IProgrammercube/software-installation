pipeline
{
       agent any
       parameters{
                string(name:'USER_NAME', defaultValue:'NA', description:'Please enter Your Name here...')
       }
        stages
        {
                 stage("User Name Info")
                {
                        steps
                        {
                                sh '''
                                          echo "Welcome User ${USER_NAME}"
                                   '''
                        }
                }
        }
}
