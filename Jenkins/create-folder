pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Run create-folder script') {
            steps {
                sh '''
                   echo 'I am in create Folder'
                '''
            }
        }
    }
}
