pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Run create-folder script') {
            steps {
                sh '''
                    ls -l Jenkins
                    chmod +x Jenkins/create-folder
                    Jenkins/create-folder
                '''
            }
        }
    }
}
